<UserControl x:Class="PropertyGrid.WPF.Demo.PropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PropertyGrid.WPF.Demo" 
             xmlns:prop="clr-namespace:PropertyGrid.WPF;assembly=PropertyGrid.WPF"
             xmlns:infr="clr-namespace:PropertyGrid.WPF.Demo.Infrastructure"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Click="refresh_click">refresh</Button>
        </ToolBar>
        <UniformGrid >
            <prop:PropertyTree
                        x:Name="PropertyTree"
                        Engine="{x:Static infr:Engine.Instance}"     
                        TreeView.SelectedItemChanged="PropertyTree_SelectedItemChanged"
                        />
            <prop:PropertyTree x:Name="ViewModelTree" />


            <Grid x:Name="ContentGrid"></Grid>

        </UniformGrid>
    </DockPanel>
</UserControl>
